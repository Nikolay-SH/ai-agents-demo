org.slf4j.simpleLogger.defaultLogLevel=warn
# Koog 1.2.0 logs Tool.Result as unsupported in the text-only conversion pass,
# then serializes the same result separately as role=tool (OllamaConverters.kt).
# Suppress this known misleading warning; our shared lab prints every tool result.
org.slf4j.simpleLogger.log.ai.koog.prompt.executor.ollama.client.dto.OllamaConverters=error
